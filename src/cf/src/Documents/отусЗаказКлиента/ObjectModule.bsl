
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
	
	СуммаДепозита = Состав.Итог("ДепозитВсего");
	СуммаАренды = Состав.Итог("АрендаВсего");
	СуммаДокумента = СуммаДепозита + СуммаАренды;
	
КонецПроцедуры
